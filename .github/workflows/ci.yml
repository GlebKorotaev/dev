name: CI-CD

on:
  push:
    branches:
      - main
      - master
    tags:
      - "v*"
  pull_request:
    branches:
      - main
      - master
  workflow_dispatch:

env:
  IMAGE_NAME: docker.io/gleb332/dev2

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: sudo apt-get update && sudo apt-get install -y build-essential dpkg-dev gcc make

      - name: Run build script
        run: |
          chmod +x ci/*.sh
          ./ci/build.sh

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: matrix
          path: out/matrix

  test:
    name: Test
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: matrix
          path: out

      - name: Make binary executable
        run: chmod +x out/matrix

      - name: Run test script
        run: |
          chmod +x ci/*.sh
          ./ci/test.sh ./out/matrix

  package:
    name: Package
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install packaging dependencies
        run: sudo apt-get update && sudo apt-get install -y build-essential dpkg-dev fakeroot

      - name: Run package script
        run: |
          chmod +x ci/*.sh
          ./ci/package.sh

      - name: Upload .deb artifact
        uses: actions/upload-artifact@v4
        with:
          name: matrix-deb
          path: dist/*.deb

  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: package
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download .deb artifact
        uses: actions/download-artifact@v4
        with:
          name: matrix-deb
          path: dist

      - name: Build Docker image from .deb
        run: |
          set -euo pipefail
          ls -la dist
          docker build -t latin-square:ci .

      - name: Test Docker image - HTTP Server
        run: |
          docker run -d --name latin-square-test -p 8080:8080 latin-square:ci python3 /usr/bin/server.py
          sleep 5
          if ! docker ps | grep -q latin-square-test; then
            echo "Container failed to start"
            docker logs latin-square-test
            exit 1
          fi
          echo "Container started successfully"
          echo "Testing health endpoint..."
          HEALTH_RESPONSE=$(curl -s http://localhost:8080/health)
          if [ "$HEALTH_RESPONSE" = "OK" ]; then
            echo "Health check passed"
          else
            echo "Health check failed"
            docker logs latin-square-test
            exit 1
          fi
          echo "Testing valid Latin square..."
          VALID_RESPONSE=$(printf "3\n1 2 3\n2 3 1\n3 1 2" | curl -s -X POST --data-binary @- http://localhost:8080/check)
          if echo "$VALID_RESPONSE" | grep -q "является латинским квадратом"; then
            echo "Valid Latin square test passed"
          else
            echo "Valid Latin square test failed"
            echo "Response: $VALID_RESPONSE"
            docker logs latin-square-test
            exit 1
          fi
          echo "Testing invalid Latin square..."
          INVALID_RESPONSE=$(printf "3\n1 2 2\n2 3 1\n3 1 2" | curl -s -X POST --data-binary @- http://localhost:8080/check)
          if echo "$INVALID_RESPONSE" | grep -q "НЕ является латинским квадратом"; then
            echo "Invalid Latin square test passed"
          else
            echo "Invalid Latin square test failed"
            echo "Response: $INVALID_RESPONSE"
            docker logs latin-square-test
            exit 1
          fi
          echo "Testing metrics endpoint..."
          METRICS_RESPONSE=$(curl -s http://localhost:8080/metrics)
          if echo "$METRICS_RESPONSE" | grep -q "latin_square"; then
            echo "Metrics endpoint test passed"
          else
            echo "Metrics endpoint test failed"
            exit 1
          fi
          docker stop latin-square-test
          docker rm latin-square-test
          echo "All Docker tests passed successfully"

      - name: Publish image to Docker Hub (tag builds only)
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          VERSION_TAG: ${{ github.ref_name }}
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: |
          set -euo pipefail
          if [ -z "${DOCKERHUB_USERNAME}" ] || [ -z "${DOCKERHUB_TOKEN}" ]; then
            echo "DOCKERHUB_USERNAME/DOCKERHUB_TOKEN are not set. Skipping Docker Hub push."
            exit 0
          fi
          echo "${DOCKERHUB_TOKEN}" | docker login -u "${DOCKERHUB_USERNAME}" --password-stdin
          docker tag latin-square:ci "${IMAGE_NAME}:${VERSION_TAG}"
          docker tag latin-square:ci "${IMAGE_NAME}:latest"
          docker push "${IMAGE_NAME}:${VERSION_TAG}"
          docker push "${IMAGE_NAME}:latest"
          echo "Successfully published to Docker Hub: ${IMAGE_NAME}:${VERSION_TAG}"

  release:
    name: Release
    runs-on: ubuntu-latest
    needs: deploy
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - name: Download package artifact
        uses: actions/download-artifact@v4
        with:
          name: matrix-deb
          path: dist

      - name: Debug - List files
        run: |
          echo "Files in dist:"
          ls -la dist/

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*.deb
          name: Release ${{ github.ref_name }}
          body: |
            ## Релиз ${{ github.ref_name }}
            
            ### Что нового:
            - Проверка матрицы на латинский квадрат
            - HTTP сервер для обработки запросов
            - Prometheus метрики для мониторинга
            - Docker образ доступен на Docker Hub: `docker pull gleb332/dev2:${{ github.ref_name }}`
            
            ### Установка и использование:
            
            **Deb пакет:**
            ```bash
            sudo dpkg -i latin-square_${{ github.ref_name }}_amd64.deb
            latin-square